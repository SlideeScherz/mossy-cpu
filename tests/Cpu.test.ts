import { Cpu } from "../src/hardware/Cpu";
import { expect } from "chai";

/*
const cpu = new Cpu(11, "Cpu.test.ts", false);

describe("CPU", () => {

  // the tests container
  it("input buffer empty", () => {
    expect(cpu.inputBuffer).to.be.empty;
  });

  it("ouput buffer empty", () => {
    expect(cpu.outputBuffer).to.be.empty;
  });

  it("clockCount init to 0", () => {
    expect(cpu.cpuClockCount).to.equal(0);
  });

  it("pulse", () => {
    expect(cpu.pulse).to.not.throw();
  });

});
*/
